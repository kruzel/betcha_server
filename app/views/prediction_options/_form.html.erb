<%= form_for([@topic_category, @topic,@prediction_option]) do |f| %>
  <% if @prediction_option.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@prediction_option.errors.count, "error") %> prohibited this prediction_option from being saved:</h2>

      <ul>
      <% @prediction_option.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field">
      <%= f.label :category %><br />
      <%= f.label @topic_category.name %>
    </div>
    <div class="field">
      <%= f.label :topic %><br />
      <%= f.label @topic.name %>
    </div>

    <div class="field">
      <%= f.label :prediction %><br />
      <%= f.text_field :name %>
    </div>

    <div class="field">
      <%= f.label :logo %><br />
      <%= f.file_field :image %>
    </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
