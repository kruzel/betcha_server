<%= form_for(@topic_category) do |f| %>
  <% if @topic_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@topic_category.errors.count, "error") %> prohibited this topic_category from being saved:</h2>

      <ul>
      <% @topic_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id %><br />
    <%= f.text_field :id %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :image %><br />
    <%= f.text_field :image %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
