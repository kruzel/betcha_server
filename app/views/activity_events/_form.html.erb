<%= form_for(@activity_event) do |f| %>
  <% if @activity_event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@activity_event.errors.count, "error") %> prohibited this activity_event from being saved:</h2>

      <ul>
      <% @activity_event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :event_type %><br />
    <%= f.text_field :event_type %>
  </div>
  <div class="field">
    <%= f.label :object_id %><br />
    <%= f.text_field :obj_id %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
