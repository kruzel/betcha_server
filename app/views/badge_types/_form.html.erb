<%= form_for(@badge_type) do |f| %>
  <% if @badge_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@badge_type.errors.count, "error") %> prohibited this badge_type from being saved:</h2>

      <ul>
      <% @badge_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :image %><br />
    <%= f.file_field :image %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
